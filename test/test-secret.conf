# {"title": "Secret Admin Panel", "category": "Admin", "isSecret": true}
server {
    listen 80;
    server_name admin-panel.local;
    
    location / {
        proxy_pass http://localhost:8080;
    }
}
